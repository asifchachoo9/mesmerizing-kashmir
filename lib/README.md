# Shared application code

Canonical location for reusable server-side modules shared by API functions.

Do not place routed endpoints here. Files in `lib/` are imported by files under `api/` or `pages/`.